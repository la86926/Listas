'use strict';
function assistantList(typeOrText){var name,arr;if(typeOrText==='desayuno'){name='Desayuno saludable';arr=['Avena','Banana','Yogur','Huevos','Aguacate']}else if(typeOrText==='limpieza'){name='Limpieza del hogar';arr=['Detergente','Guantes','Limpiavidrios','Papel higiénico','Jabón']}else if(typeOrText==='parrilla'){name='Parrilla con amigos';arr=['Pollo','Cerveza','Tomate','Cebolla','Papas','Agua']}else{var q=typeOrText||'Lista sugerida';name=q;var low=q.toLowerCase();if(low.indexOf('cena')>=0||low.indexOf('almuerzo')>=0)arr=['Arroz','Pollo','Tomate','Cebolla','Agua'];else if(low.indexOf('limpieza')>=0)arr=['Detergente','Guantes','Limpiavidrios','Papel higiénico'];else arr=['Pan','Leche','Huevos','Fruta','Agua']}var l={id:uid(),name:name,members:[],items:arr.map(function(n){var p=catalog.find(function(x){return x[0]===n});return item(n,false,p?p[2]:'Sin categoría')})};db.lists.unshift(l);save();toast('Lista creada: '+name);st.tab='lists';st.screen='list';st.listId=l.id;render()}
var wakeLock=null;function setWake(on){db.settings.awake=on;save();if(on&&navigator.wakeLock&&navigator.wakeLock.request){navigator.wakeLock.request('screen').then(function(lock){wakeLock=lock;toast('Pantalla encendida activada')}).catch(function(){db.settings.awake=false;save();toast('El dispositivo no permitió mantener la pantalla encendida');render()})}else if(on){db.settings.awake=false;save();toast('Esta función no está disponible en este navegador');render()}else if(wakeLock){try{wakeLock.release()}catch(e){}wakeLock=null}}
function toast(t){var old=document.querySelector('.toast');if(old)old.remove();var d=document.createElement('div');d.className='toast';d.textContent=t;document.body.appendChild(d);setTimeout(function(){if(d.parentNode)d.parentNode.removeChild(d)},1800)}
function action(a){var l=listById(st.listId),x;if(a==='home-menu'){st.menu=!st.menu;render()}else if(a==='new-list'){st.modal='new-list';render()}else if(a==='trash'){st.menu=false;st.screen='trash';render()}else if(a==='settings'){st.tab='lists';st.menu=false;st.screen='settings';render()}else if(a==='appearance'){st.tab='lists';st.screen='appearance';render()}else if(a==='categories'){st.screen='categories';render()}else if(a==='expenses'){st.screen='expenses';render()}else if(a==='faq'){st.tab='lists';st.screen='faq';render()}else if(a==='back'){if(st.tab==='products'&&st.screen==='product-detail'){st.screen='products'}else if(st.screen==='list'||st.screen==='settings'||st.screen==='trash'){st.screen='home'}else if(st.screen==='appearance'||st.screen==='categories'||st.screen==='expenses'||st.screen==='faq'){st.screen='settings'}render()}else if(a==='list-menu'){st.modal='list-menu';render()}else if(a==='close-modal'){st.modal=null;st.modalData=null;render()}else if(a==='search-list'){st.modal=null;st.listQuery=' ';render();setTimeout(function(){var p=document.getElementById('listSearch');if(p){p.value='';st.listQuery='';p.focus()}},0)}else if(a==='close-search'){st.listQuery='';render()}else if(a==='share'){st.modal='share';render()}else if(a==='sort'){st.modal='sort';render()}else if(a==='toggle-done'){st.showDone=!st.showDone;render()}else if(a==='add-item'){st.modal='add-item';st.modalData={q:''};render()}else if(a==='add-typed-item'){var inp=document.getElementById('addItemInput'),n=inp?inp.value.trim():'';if(!n)return toast('Escribe un artículo');addCatalogItem(n,l);st.modal=null;st.modalData=null;render()}else if(a==='check-all'){l.items.forEach(function(i){i.done=true});st.modal=null;save();render()}else if(a==='uncheck-all'){l.items.forEach(function(i){i.done=false});st.modal=null;save();render()}else if(a==='delete-purchased'){st.modal='confirm';st.modalData={title:'Eliminar artículos comprados',text:'Se eliminarán todos los artículos marcados de esta lista.',action:'delete-purchased'};render()}else if(a==='delete-current-list'){st.modal='confirm';st.modalData={title:'Mover lista a la papelera',text:'Podrás restaurarla más adelante desde la Papelera.',action:'delete-current-list'};render()}else if(a==='rename-list'){st.modal='rename-list';st.modalData={id:st.modalData.id};render()}else if(a==='duplicate-list'){var src=listById(st.modalData.id),cp=clone(src);cp.id=uid();cp.name=src.name+' copia';cp.items.forEach(function(i){i.id=uid()});db.lists.unshift(cp);save();st.modal=null;toast('Lista duplicada');render()}else if(a==='delete-list-from-home'){st.modal='confirm';st.modalData={title:'Mover lista a la papelera',text:'Podrás restaurarla desde la Papelera.',action:'delete-list-home:'+st.modalData.id};render()}else if(a==='empty-trash'){if(!db.trash.length)return toast('La papelera ya está vacía');st.modal='confirm';st.modalData={title:'Vaciar papelera',text:'Esta acción eliminará definitivamente todas las listas de la papelera.',action:'empty-trash'};render()}else if(a==='new-category'){st.modal='new-category';render()}else if(a==='toggle-notifications'){if(db.settings.notifications){db.settings.notifications=false;save();toast('Notificaciones desactivadas');render()}else if('Notification' in window&&Notification.requestPermission){Notification.requestPermission().then(function(p){db.settings.notifications=p==='granted';save();toast(db.settings.notifications?'Notificaciones activadas':'No se concedió permiso para notificaciones');render()}).catch(function(){toast('No se pudo solicitar permiso para notificaciones')})}else{toast('Las notificaciones no están disponibles en este navegador')}}else if(a==='report'){location.href='mailto:?subject='+encodeURIComponent('Problema en Listas')+'&body='+encodeURIComponent('Describe aquí el problema:')}else if(a==='feedback'){location.href='mailto:?subject='+encodeURIComponent('Comentarios sobre Listas')+'&body='+encodeURIComponent('Comentario:')}else if(a==='refresh'){save();toast('Listas actualizadas')}else if(a==='icon-info'){toast('El navegador controla el icono del acceso directo')}else if(a==='edit-email'){st.modal='edit-email';render()}else if(a==='rate'){toast('¡Gracias por tu valoración!')}else if(a==='assistant-go'){var ai=document.getElementById('assistantInput'),q=ai?ai.value.trim():'';if(!q)return toast('Escribe qué necesitas');assistantList(q)}else if(a==='add-product-to-list'){st.modal='select-list';render()}else if(a==='add-member'){var em=document.getElementById('memberEmail'),v=em?em.value.trim():'';if(!v||v.indexOf('@')<1)return toast('Ingresa un correo válido');l.members=l.members||[];if(l.members.indexOf(v)>=0)return toast('Ese correo ya está añadido');l.members.push(v);save();render();toast('Colaborador guardado localmente')}else if(a==='save-item'){x=l.items.find(function(i){return i.id===st.modalData.id});if(!x)return;var n=document.getElementById('editName').value.trim();if(!n)return toast('El nombre no puede estar vacío');x.name=n;x.category=document.getElementById('editCategory').value;x.price=Math.max(0,parseFloat(String(document.getElementById('editPrice').value).replace(',','.'))||0);x.qty=Math.max(1,parseInt(document.getElementById('editQty').value,10)||1);if(db.settings.rememberPrice&&x.price)db.priceMemory[x.name]=x.price;save();st.modal=null;st.modalData=null;render();toast('Artículo actualizado')}else if(a==='delete-item'){st.modal='confirm';st.modalData={title:'Eliminar artículo',text:'Este artículo se eliminará de la lista.',action:'delete-item:'+st.modalData.id};render()}else if(a==='move-up'||a==='move-down'){var id=st.modalData.id,idx=l.items.findIndex(function(i){return i.id===id}),to=a==='move-up'?idx-1:idx+1;if(to<0||to>=l.items.length)return;var temp=l.items[idx];l.items[idx]=l.items[to];l.items[to]=temp;db.settings.sort='custom';save();render();toast('Orden actualizado')}}
